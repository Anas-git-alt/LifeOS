# LifeOS Data Root

Phase 1 canonical runtime root.

- `data/manifest.json` is generated by backend at startup.
- Runtime SQLite should move toward `data/sqlite/`.
- Workspace archives should move toward `data/workspace/archives/`.
- Legacy `storage/` paths still work during migration and are recorded in manifest.
