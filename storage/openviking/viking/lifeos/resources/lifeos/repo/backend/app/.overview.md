# app

The `app` directory serves as the core Python package for the LifeOS application, a FastAPI-based backend system designed to manage personal life, spirituality, and productivity workflows. This module encapsulates the central business logic, database interactions, API routing, and configuration management. It covers essential domains such as agent management, prayer tracking, workspace synchronization, and experiment runs. Key components include SQLAlchemy ORM models, Pydantic schemas for data validation, and async database handling with SQLite support. This directory is suitable for backend developers maintaining the API, contributors adding new features, or system architects reviewing the application's structural design.

## Quick Navigation

**What do you want to do?**

*   **Configure the application?**
    → Set environment variables and secrets in config.py `config.py`
*   **Understand the database structure?**
    → Review ORM models and schemas in models.py `models.py`
    → Check async engine setup in database.py `database.py`
*   **Secure an API endpoint?**
    → Implement token validation using security.py `security.py`
*   **Initialize or troubleshoot the server?**
    → Inspect the application entry point in main.py `main.py`
*   **Explore specific features?**
    → Navigate to `routers/` for API endpoints
    → Navigate to `services/` for business logic
    → Navigate to `migrations/` for database schema changes

## Detailed Description

### __init__.py `__init__.py`
This file acts as the package initializer for the `app` module. It typically defines the package scope and may import key dependencies or expose public interfaces for the rest of the application to consume.

### config.py `config.py`
This module handles LifeOS configuration management. It utilizes `pydantic_settings.BaseSettings` to load secrets and environment variables, likely from a `.venv/.env` file. The `Settings` class provides properties for CORS origins, owner IDs, and paths for workspace repositories and archives. It also normalizes memory backend settings and manages API keys, serving as the central source of truth for application configuration.

### database.py `database.py`
Responsible for database initialization and management, this module sets up the asynchronous SQLAlchemy engine and session maker for SQLite. It includes a `Base` class for ORM models and functions to apply SQL migrations, initialize the database, and provide async session dependencies for FastAPI routes. This ensures the database schema is up-to-date and ready for use.

### main.py `main.py`
This is the entry point for the LifeOS FastAPI application. It assembles the web server, configuring CORS middleware and integrating various routers for agents, approvals, events, and other features. The `lifespan` async context manager handles startup routines—such as database initialization, scheduler bootstrapping, and self-checks—and manages graceful shutdown for services like the OpenViking client and scheduler.

### middleware.py `middleware.py`
This file is designated for request processing middleware. While specific implementation details are not provided, it typically handles cross-cutting concerns such as request context injection, logging, or custom header processing before requests reach the route handlers.

### models.py `models.py`
A comprehensive module defining the data architecture. It contains SQLAlchemy ORM classes (e.g., `Agent`, `VoiceSession`, `PrayerWindow`, `ExperimentRun`) mapping to database tables and Pydantic schemas (e.g., `AgentCreate`, `ChatRequest`) for API data validation. This file bridges the gap between the relational database structure and the JSON payloads used in API communication.

### redaction.py `redaction.py`
This module is intended for data redaction logic. It likely contains utilities to filter or obscure sensitive information (such as API keys or personal data) before logging or transmitting data to external systems, ensuring pr